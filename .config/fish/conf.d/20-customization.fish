# -----------------------------------------------------
# CUSTOMIZATION
# -----------------------------------------------------

# -----------------------------------------------------
# Prompt
# -----------------------------------------------------
#eval "$($HOME/.local/bin/oh-my-posh init fish --config $HOME/.config/ohmyposh/zen.toml)"
eval "$($HOME/.local/bin/oh-my-posh init fish --config $HOME/.config/ohmyposh/pene.omp.json)"

# Forzar colores de Dracula al inicio
fish_config theme choose "Dracula Official"
